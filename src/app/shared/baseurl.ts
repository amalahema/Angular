//this is the baseURL at which my JSON server is accessible.
//open the two terminal one for json server
//other for ng serve --open
//Instead of fetch from dishservices we use json server db as a database to load array of dis objects.
/*HTTP client in order to obtain the data 
from the server site and then use the data in
constructing the views within the components of our Angular application.*/
export const baseURL = 'http://localhost:3000/';


